# Local Flexiv/Isaac runtime environment for Stage1 validation.
# Relative paths in this file are resolved from configs/environments/.
# External runtimes vary by machine, so provide them with the documented
# environment variables or CLI flags instead of committing absolute paths:
# FLEXIV_RDK_PYTHON, ISAAC_PYTHON, ISAACSIM_ROOT, and STUDIO_ROOT.

isaac_sim_ws: "../../../../isaac_sim_ws"
record_output_root: "../../datasets/stage1_records"
lerobot_output_root: "../../datasets/lerobot"
